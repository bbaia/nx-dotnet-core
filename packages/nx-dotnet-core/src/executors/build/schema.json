{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Build executor",
  "description": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project to publish. If a file is not specified, the command will search the current directory for one."
    },
    "outputPath": {
      "type": "string",
      "description": "The output directory to place the generated artifacts in."
    },
    "configuration": {
      "type": "string",
      "description": "The configuration to build for. The default for most projects is 'Debug'."
    },
    "noRestore": {
      "type": "boolean",
      "description": "Do not restore the project before building your project.",
      "default": false
    }
  },
  "required": ["project", "outputPath"]
}
